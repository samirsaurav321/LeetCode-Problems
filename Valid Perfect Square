"""
Given a positive integer num, write a function which returns True if num is a perfect square else False.

Note: Do not use any built-in library function such as sqrt.

Example 1:

Input: 16
Output: true
Example 2:

Input: 14
Output: false
"""

import math
class Solution:
    def isPerfectSquare(self, num):
        """
        :type num: int
        :rtype: bool
        """
        if num==0:
            return True
        x= str(math.sqrt(num))
        if (x[-1] == '0'):
             return True

        return False
        
 if __name__=='__main__':

    s = Solution

    num = 14
    print(s.isPerfectSquare(s,num))
    
    # output --> False
